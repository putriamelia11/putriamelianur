.App {
  text-align: center;
  margin-top: 50px;
}

.color-options {
  display: flex;
  justify-content: center;
  margin-top: 20px;
}

button {
  border-radius: 5px;
  font-size: 16px;
  font-weight: bold;
  transition: all 0.3s ease;
}

button:hover {
  opacity: 0.8;
}
